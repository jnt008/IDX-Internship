# IDX-Internship

## Setup

1. Install Docker
2. Run MySQL container
3. Download rets_property.sql and rets_openhouse.sql from company FTP
4. Import the files

## Database

Tables:
- rets_property
- rets_openhouse

## Verification

SELECT COUNT(*) FROM rets_property;
SELECT COUNT(*) FROM rets_openhouse;
